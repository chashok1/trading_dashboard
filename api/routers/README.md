# api/routers/

Reserved for the eventual split of `api/main.py` into FastAPI routers.
Currently empty by design. See the project's code review for the planned
split (health, dash, ref, rules, trace, pages).
